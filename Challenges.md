
### Phase 1: Beginner (Basic Selection & Filtering)

*Goal: Understand table structures and simple WHERE clauses.*

1. Select all columns from the `Products` table.
2. List the first and last names of all `Customers`.
3. Find all products where the `UnitPrice` is greater than 10.
4. List all ingredients where the `UnitOfMeasure` is 'kg'.
5. Find all customers who do not have an email address recorded.
6. Select all products that are gluten-free (`IsGlutenFree = 1`).
7. List orders that have a status of 'Pending'.
8. Show all ingredients with `CurrentStock` less than 5.
9. Find categories with names starting with the letter 'C'.
10. Get all orders placed in the last 30 days.

---

### Phase 2: Intermediate (Joins & Aggregations)

*Goal: Combine tables and perform basic calculations.*

11. Display `ProductName` alongside its `CategoryName`.
12. List all orders showing the `FirstName` and `LastName` of the customer who placed them.
13. Calculate the total number of products in each category.
14. Find the average `UnitPrice` of all products.
15. List each order ID and the total number of items in that order (count from `OrderDetails`).
16. Show all ingredients used in a specific product (e.g., 'Chocolate Cake').
17. Find the total sum of `TotalAmount` for all 'Completed' orders.
18. List products that have never been ordered (LEFT JOIN with `OrderDetails`).
19. Display the total cost of ingredients currently in stock (`CurrentStock * CostPerUnit`).
20. Show the top 5 most expensive products.

---

### Phase 3: Advanced (Subqueries, Grouping & Logic)

*Goal: Complex filtering and multi-level data processing.*

21. Find customers who have placed more than 5 orders.
22. List products that have a unit price higher than the average unit price of all products.
23. Calculate the total revenue generated by each category.
24. Find the ingredient that is used in the highest number of recipes.
25. Identify orders where the `TotalAmount` does not match the sum of `LineTotal` in `OrderDetails`.
26. List all products that use 'Sugar' as an ingredient.
27. Find the customer who has spent the most money in total.
28. Show a list of categories and the most expensive product within each category.
29. Calculate the total "potential cost" to make one of every product based on `Recipes` and `CostPerUnit`.
30. Find orders that contain at least one gluten-free product.

---

### Phase 4: Expert (CTEs, Window Functions & Optimization)

*Goal: Analytical reporting and complex transformations.*

31. **CTE:** Create a Common Table Expression to find the total quantity of each ingredient needed for all 'Pending' orders.
32. **Window Function:** Rank products by price within their respective categories.
33. **Running Total:** Show a list of orders by date with a running total of `TotalAmount`.
34. **Inventory Alert:** Create a query that identifies products that *cannot* be made because ingredient `CurrentStock` is lower than `QuantityRequired`.
35. **Pivot:** Create a report showing total sales per category, broken down by month.
36. **Data Integrity:** Find any duplicate ingredients (same name, different ID).
37. **Profit Margin:** Calculate the profit margin for each product (`UnitPrice` minus the sum of ingredient costs).
38. **Customer Retention:** Find customers who placed an order in 2023 but have not placed one in 2024.
39. **Transaction Simulation:** Write a script to update `CurrentStock` in `Ingredients` when an order status changes to 'Shipped'.
40. **Complex Join:** List every customer, their last order date, the name of the most expensive product in that order, and the total they have ever spent.

---
### Phase 5: Advanced String & Date Manipulation (Questions 41–70)

*Focus: Formatting and time-based logic.*

41. Format all customer names as "LASTNAME, Firstname".
42. Extract the domain name from all customer emails (everything after the '@').
43. Find orders placed specifically on a Saturday or Sunday.
44. List products whose names contain the word "Cake" (case-insensitive).
45. Find the age of each order in days relative to today.
46. Create a "low stock" flag: Display `IngredientName` and a column 'Status' that says 'REORDER' if stock is < 10, else 'OK'.
47. Show the first 3 characters of every category name in uppercase.
48. List customers whose `LastName` starts with 'S' and ends with 'n'.
49. Find the date of the very first order ever placed in the system.
50. Calculate the number of months between the first order and the last order.
51. Replace all NULL emails with the string 'No Email Provided'.
52. Find the total number of orders placed in the year 2024.
53. List orders placed between 9:00 AM and 5:00 PM (Business Hours).
54. Show the `OrderDate` formatted as "Day, Month DD, YYYY".
55. Find ingredients with names exactly 5 characters long.
56. Use `CHARINDEX` to find the position of the '@' symbol in customer emails.
57. Generate a username for customers by taking the first 3 letters of `FirstName` and the last 3 of `LastName`.
58. Find the most recent order for each specific customer.
59. List products that do **not** have "Organic" in their name.
60. Calculate the total revenue for "Q1" (January through March) of any year.

---

### Phase 6: Set Theory & Complex Joins (Questions 71–100)

*Focus: UNION, INTERSECT, EXCEPT, and Multi-way Joins.*

61. Use `UNION` to create a list of all unique names (Customer names and Ingredient names combined).
62. Find products that belong to either the 'Bakery' category OR the 'Dessert' category.
63. List all products and their ingredients, including products that have no ingredients listed (Left Join).
64. Find ingredients that are used in *every* product within a specific category.
65. Display a list of customers who have bought both a 'Gluten-Free' item and a 'Regular' item.
66. Use `EXCEPT` to find ingredients that are currently in stock but have never been used in a recipe.
67. Perform a `CROSS JOIN` between `Categories` and `Customers` to see every potential marketing segment.
68. Show the `OrderID`, `ProductName`, and `IngredientName` in a single flat list (Joining 5 tables).
69. Find customers who have placed orders but never purchased a product from the 'Beverage' category.
70. Create a self-join on the `Ingredients` table to find pairs of ingredients with the same `CostPerUnit`.
71. Find the "cheapest" ingredient used in the "most expensive" product.
72. List all `OrderDetails` for orders that are currently 'In Progress'.
73. Display total quantity of 'Flour' required to fulfill all 'Pending' orders.
74. Find products where the cost of ingredients is higher than the `UnitPrice` (Negative Profit).
75. Identify categories that have no products assigned to them.

---

### Phase 7: Data Modeling & DML (Questions 101–130)

*Focus: Modifying data and schema constraints.*

76. Insert a new category named 'Holiday Specials'.
77. Update all 'Pending' orders to 'Cancelled' if they were placed more than a year ago.
78. Increase the `UnitPrice` of all Gluten-Free products by 15%.
79. Delete a customer record, but first, check for foreign key violations in the `Orders` table.
80. Add a new column `PhoneNumber` to the `Customers` table.
81. Create a `VIEW` called `v_ProductCosts` that shows Product names and their total ingredient costs.
82. Write a script to "Soft Delete" a product by adding an `IsActive` bit column and setting it to 0.
83. Update the `CurrentStock` of 'Milk' by subtracting the amount used in the latest order.
84. Insert a new order with 3 line items using a single transaction.
85. Create a `UNIQUE` constraint on the `Email` column in the `Customers` table.

---

### Phase 8: Advanced Analytics (Questions 131–170)

*Focus: Window Functions, Paging, and Math.*

86. Use `ROW_NUMBER()` to select the 10th through 20th most expensive products (Pagination).
87. Calculate the percentage contribution of each product to the total order value.
88. Find the "Median" price of products using `PERCENTILE_CONT`.
89. Use `LEAD()` and `LAG()` to show the `TotalAmount` of the current order vs. the previous order for each customer.
90. Group customers into 4 tiles (Quartiles) based on their total spending using `NTILE(4)`.
91. Calculate the standard deviation of ingredient costs.
92. Find the "Moving Average" of order totals over the last 3 orders.
93. Rank categories by their total quantity of items sold.
94. Calculate the "Inventory Turnover" (Total used / Average stock) for each ingredient.
95. Use `RANK()` vs `DENSE_RANK()` on product prices to see the difference in handling ties.

---

### Phase 9: Database Logic & Expert Challenges (Questions 171–200)

*Focus: Optimization, Logic, and Business Intelligence.*

96. Write a query to find "Ghost Orders" (Orders with a `TotalAmount` > 0 but no rows in `OrderDetails`).
97. Find the day of the week that generates the most revenue.
98. Identify the "Best Selling Pair" (Two products that are most often bought together in the same order).
99. Create a recursive CTE (if applicable) or a logic loop to find the total "depth" of a recipe.
100. **The Master Query:** Generate a year-end report: For every customer, show Total Spent, Favorite Category, Total Orders, and the date of their last purchase, sorted by Total Spent descending.

---

### Phase 10: Performance, Indexing & Metadata (Questions 101–125)

*Focus: Understanding how the engine handles your data.*

101. Write a query to find the storage size of each table in the `management_system`.
102. Identify which columns would benefit most from a **Non-Clustered Index** based on frequent `WHERE` clause usage.
103. List all foreign key constraints in the database using `INFORMATION_SCHEMA`.
104. Write a query to find all tables that contain a column named 'Price' or 'Amount'.
105. Check for "Fragmentation" levels in the indexes of the `OrderDetails` table.
106. Generate a script that drops and recreates all foreign keys (useful for bulk data loading).
107. Find the "Execution Plan" for a query joining all 7 tables and identify the most expensive operator.
108. Determine the memory usage of the last 10 queries executed against this DB.
109. Create a **Filtered Index** for the `Orders` table where `Status = 'Pending'`.
110. List all columns in the `Customers` table that allow `NULL` values.

---

### Phase 11: Data Integrity & Auditing (Questions 126–150)

*Focus: Catching errors and ensuring "One Version of the Truth."*

111. Find all `OrderDetails` records that point to a `ProductID` which no longer exists (Orphaned records).
112. Identify customers who have duplicate email addresses but different `CustomerIDs`.
113. Find ingredients where the `CurrentStock` is a negative number.
114. Detect orders where the `OrderDate` is in the future (logical error).
115. Check the `Recipes` table for any products that have **zero** ingredients assigned.
116. Find line items in `OrderDetails` where the `LineTotal` is not equal to `Quantity * UnitPrice`.
117. Identify any `CategoryName` that is a duplicate or a near-duplicate (e.g., 'Bakery' vs 'Bakeries').
118. Create a "Shadow Table" for `Price` history to track every time a `UnitPrice` changes.
119. Write a query to find the "Longest Gap" (in days) between any two orders in the system.
120. List all tables that do not have a Primary Key defined.

---

### Phase 12: Business Intelligence & Forecasting (Questions 151–175)

*Focus: Predicting trends and summarizing performance.*

121. **Seasonality:** Calculate total sales for each day of the week across the last 2 years.
122. **ABC Analysis:** Classify ingredients into 'A' (High Value/High Usage), 'B', and 'C' tiers.
123. **Churn Rate:** Find the percentage of customers who ordered last month but didn't order this month.
124. **Ingredient Dependency:** Which ingredient, if it ran out, would stop the production of the most number of products?
125. **Average Basket Size:** Calculate the average number of products per order.
126. **Price Elasticity:** Compare the total quantity sold of a product before and after a price increase.
127. **Vendor Load:** If each ingredient came from a different vendor, which vendor is most critical based on total spend?
128. **Batch Planning:** Calculate how many units of "Product X" can be made given current stock levels.
129. **Customer Lifetime Value (CLV):** Calculate the total revenue generated by each customer since their first order.
130. **Top Categories:** List the top 3 categories by profit margin, not just total sales.

---

### Phase 13: Procedural Logic & Scripting (Questions 176–200)

*Focus: T-SQL scripts, Loops, and Variables.*

131. Declare a variable `@TargetDate` and find all orders placed after that date.
132. Write a `WHILE` loop that prints the names of all categories one by one.
133. Create a **Stored Procedure** `usp_GetCustomerHistory` that takes a `CustomerID` as a parameter.
134. Create a **User Defined Function (UDF)** to calculate the total cost of a product's recipe.
135. Write a **Trigger** that automatically updates `CurrentStock` in the `Ingredients` table whenever a new `OrderDetails` row is inserted.
136. Use `TRY...CATCH` logic to handle a division-by-zero error in a report.
137. Create a **Global Temporary Table** `##TempSales` and populate it with today's orders.
138. Write a script to export the `Customers` table into a JSON format.
139. Use `OFFSET...FETCH` to create a stored procedure for server-side pagination.
140. Create a **View** that joins `Orders`, `Products`, and `Customers` for easy reporting.
141. Write a dynamic SQL query that allows a user to filter by any column name passed as a string.
142. Implement a "Transaction Savepoint" while updating multiple table rows.
143. Write a query using `FOR XML PATH` to concatenate all ingredients for a product into a single comma-separated string.
144. Find the "2nd Highest" spender among customers without using `TOP`.
145. **The Ultimate Challenge:** Write a script that detects "Low Stock" for all ingredients needed for all 'Pending' orders and generates a 'Purchase Order' list in a temporary table.

---

### Phase 14: Recursive CTEs & Hierarchy (Questions 151–165)

*Focus: Navigating relationships where data refers back to itself.*

151. **Recipe Scaling:** Write a query to calculate the total ingredient requirements if you needed to fulfill 100 units of every product in the `Products` table.
152. **Category Hierarchy:** If `Categories` had a `ParentCategoryID`, write a recursive CTE to show the full path of a sub-category (e.g., "Food > Bakery > Cakes").
153. **Inventory Depletion:** Calculate how many days of stock are left for 'Sugar' based on the average daily consumption over the last 3 months.
154. **Sequential Gap Analysis:** Find missing `OrderID` numbers in the sequence (to check for deleted records).
155. **Multi-level Recipes:** If a "Product" could be an "Ingredient" for another product (e.g., Bread used in a Sandwich), how would you structure a query to find the base raw materials?

---

### Phase 15: Security, Permissions & Compliance (Questions 166–180)

*Focus: Protecting the data and tracking access.*

156. Write a script to create a "Read-Only" user role for the `management_system` database.
157. Grant `SELECT` permissions on the `Products` table but deny access to the `CostPerUnit` column in `Ingredients`.
158. Write a query to find which database users have `sysadmin` or `db_owner` privileges.
159. Create a **Database Audit Specification** script to track whenever a user deletes a record from the `Orders` table.
160. Mask the `Email` column so that a standard clerk can only see the first letter and the domain (e.g., `j***@gmail.com`).
161. Identify the last time each table was updated using `sys.dm_db_index_usage_stats`.
162. Write a script to encrypt the `TotalAmount` column using a symmetric key.
163. Find all stored procedures that haven't been executed in the last 6 months.
164. Implement "Row-Level Security" so a manager can only see orders from their specific region (assuming a `RegionID` existed).
165. Create a trigger that prevents any product from having a `UnitPrice` of $0.00.

---

### Phase 16: Extreme SQL Challenges (Questions 181–200)

*Focus: Logic puzzles and high-level optimization.*

186. **The Peak Hour:** Identify the specific hour of the day across the entire history of the database that had the highest volume of orders.
187. **The "Cold" Customer:** Find customers who haven't ordered in 6 months, but spent more than the average customer when they were active.
188. **Stock Simulation:** Write a query that predicts which ingredients will run out first based on current "Pending" orders.
189. **Ingredient Correlation:** Find pairs of ingredients that are almost always used together in recipes (e.g., Flour and Yeast).
190. **String Aggregation:** List every `CategoryName` and, in a single cell next to it, a comma-separated list of all products in that category.
191. **Dynamic Pivot:** Create a sales report where the columns are the 12 months of the year, without hardcoding the months.
192. **Outlier Detection:** Find orders where the `TotalAmount` is more than 3 standard deviations away from the average order total.
193. **Update via Join:** Write a single `UPDATE` statement that decreases the `CurrentStock` of all ingredients used in Order #5001.
194. **The "Pareto" Query:** Identify the top 20% of products that generate 80% of the total revenue.
195. **Duplicate Logic:** Find "near-duplicate" customers—those with the same `LastName` and `Email` but different `FirstName`.
196. **Deadlock Simulation:** Describe a scenario using your schema where two transactions would cause a deadlock.
197. **JSON Parsing:** If the `Orders` table had a `Notes` column containing JSON, write a query to extract a specific key called `"DeliveryInstructions"`.
198. **The "First-Time" Impact:** Calculate the average value of a customer's *first* order versus their *second* order.
199. **Zero-Waste Check:** List products whose ingredients' `UnitOfMeasure` is 'Units' and ensure the `QuantityRequired` is always a whole number.
200. **The Self-Healing Script:** Write a script that checks for common issues (orphaned rows, negative stock, missing prices) and outputs a "Health Report" table.

---

